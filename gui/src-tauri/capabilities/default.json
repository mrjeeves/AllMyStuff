{
  "$schema": "https://schema.tauri.app/capabilities/2",
  "identifier": "default",
  "description": "Default capabilities for the AllMyStuff GUI (the main window and every per-machine console, terminal and files window, each room call window, each video-popout window, the CEC Console window, and each per-customer CEC chat window)",
  "windows": [
    "main",
    "console-*",
    "terminal-*",
    "files-*",
    "room-*",
    "video-*",
    "cec-console",
    "chat-*"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "core:window:allow-close",
    "core:window:allow-destroy",
    "core:window:allow-set-fullscreen",
    "core:window:allow-is-fullscreen",
    "core:window:allow-set-focus",
    "core:window:allow-set-cursor-grab",
    "core:window:allow-set-cursor-visible",
    "core:window:allow-set-cursor-position",
    "shell:allow-open",
    "dialog:default",
    "clipboard-manager:allow-read-text",
    "clipboard-manager:allow-write-text"
  ]
}
